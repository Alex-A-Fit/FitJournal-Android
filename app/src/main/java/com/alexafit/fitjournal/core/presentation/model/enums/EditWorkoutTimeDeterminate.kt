package com.alexafit.fitjournal.core.presentation.model.enums

// Used to determine if user is editing one of the workout time fields
enum class EditWorkoutTimeDeterminate {
    HOUR,
    MINUTE,
    SECOND
}
